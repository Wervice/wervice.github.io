function encode(string, k) {
    var whls = 0
    var leg = string.length
    while (whls != leg) {
        var string = string
        var whls = whls+1
        var cc = string.charCodeAt(whls)
        console.log(cc)

    }
}